appId: com.cardselling.agent
---
- runFlow:
    when:
      visible: Allow CS Agent to access this device’s location?
    commands:
      - assertVisible: While using the app
      - tapOn: While using the app
